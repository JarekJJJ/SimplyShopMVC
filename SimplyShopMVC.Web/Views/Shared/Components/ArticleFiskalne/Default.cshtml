@model SimplyShopMVC.Application.ViewModels.Article.ListArticleForListVm
@if (Model.Articles != null)
{
                string thumbImage = "";
            @foreach (var item in Model.Articles)
            {
                thumbImage = item.imagePath.FirstOrDefault();
                   
                        
        <div class="card-body pt-2">
            <p class="card-text">@(Html.Raw(item.Content))</p>
            <p class="card-text"><small class="text-body-secondary">Utworzono: @(item.Created.ToShortDateString()) Tytuł: @(item.Title)</small></p>
        </div>
            }
}
@section Scripts
    {
   <script type="text/javascript">       
       
    </script>
    }